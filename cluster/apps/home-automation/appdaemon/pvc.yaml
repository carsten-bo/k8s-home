---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appdaemon-local-path-claim
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 256Mi
